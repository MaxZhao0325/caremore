import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    marginVertical: 10,
  },
  name: {
    fontSize: 24,
    fontWeight: "bold",
  },
  recommended: {
    fontSize: 14,
    color: "green",
  },
});
export default styles;
